# Local development
VITE_API_BASE_URL=http://localhost:8080

# For a deployed frontend, replace this value with the public HTTPS URL of the
# deployed Spring Boot service and rebuild the client. Vite reads this value at
# build time; changing it in the hosting dashboard alone does not update an
# already-built site.
